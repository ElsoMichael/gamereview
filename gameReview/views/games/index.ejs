<div class="container moreSpace">
  <div class="row">
    <% list.forEach((game) => { %>
      <div class="col-lg-12 game-search-spacing">
        <a href="/"><img src="<%= game.cover.url %>"></a>
      </div>
    <% }); %>
  </div>
</div>